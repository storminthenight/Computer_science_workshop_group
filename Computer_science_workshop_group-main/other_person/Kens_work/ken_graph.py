#def main():
#    print("Ken Graph")
#    def load stats_csvstats_path: Path) - > pd.DataFrame:
#    rous: List Dict [str, str]] = (]
#    with stats path.open newlin=, encoding="utf-8") as file obj:
#    reader = cv. reader (file obj
#    header = next (reader, None)
#    for row in reader: if not row: continue
#    trinmed row = row: len(header)]
#    if len(trimed row) < Len(header):
#    trimmed row.extend ( * (len(header) - len(trimmed ro))
#    rows. append dict ipheader, trinmed row))